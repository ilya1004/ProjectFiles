import React from "react";

export default class Move extends React.Component {
    constructor(props, from_to, color) {
        super(props);
        this.from_to = from_to;
        this.color = color;
    }


}